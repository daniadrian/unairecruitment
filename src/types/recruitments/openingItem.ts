// One row of the public list of open roles (UC-05), prepared by the screen for the client-side
// register. `postedLabel` is the already formatted creation date.
export interface OpeningItem {
    id: string
    title: string
    division: string
    description: string
    postedLabel: string
}
